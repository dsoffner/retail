{
    "<%=page_item%>": [{
            <% if(data.length){
                for(var i = 0;i < data.length;i++) {
                if (i > 0) { %>
                }, {
                <% } else { %>
                <% } %>
                "id": "<%=data[i].id%>",
                "title": "<%=data[i].title%>",
                "type": "<%=data[i].type%>",
                "description": "<%=data[i].description%>",
                "height": "<%=data[i].height%>",
                "width": "<%=data[i].width%>",
                "price": "<%=data[i].price%>",
                "rating": "<%=data[i].rating%>"
                <% }
             } else { %>
            <% } %>
        }]
}
